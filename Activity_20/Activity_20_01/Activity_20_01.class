import java.io.File;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Scanner;

public class Activity_20_01 {
    public Activity_20_01() {
    }

    public static void main(String[] var0) throws Exception {
        if (var0.length != 1) {
            System.out.println("Usage: java Activity_20_01 TextFile");
            System.exit(1);
        }

        File var1 = new File(var0[0]);
        if (!var1.exists()) {
            System.out.println("The file " + var0[0] + " does not exist.");
            System.exit(2);
        }

        ArrayList var2 = new ArrayList();
        Scanner var3 = new Scanner(var1);
        Throwable var4 = null;

        try {
            while(var3.hasNext()) {
                String[] var5 = var3.nextLine().split(" ");

                for(int var6 = 0; var6 < var5.length; ++var6) {
                    if (var5[var6].length() > 0 && Character.isLetter(var5[var6].charAt(0))) {
                        var2.add(var5[var6]);
                    }
                }
            }
        } catch (Throwable var14) {
            var4 = var14;
            throw var14;
        } finally {
            if (var3 != null) {
                if (var4 != null) {
                    try {
                        var3.close();
                    } catch (Throwable var13) {
                        var4.addSuppressed(var13);
                    }
                } else {
                    var3.close();
                }
            }

        }

        Collections.sort(var2);
        System.out.println(var2);
    }
}